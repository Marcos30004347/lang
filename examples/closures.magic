f :: (x:i32) -> i32 {
	return 0;
}

iter :: (x:i32, p:i32, f:i32) -> i32 {
	h :: (a:i32, r:i32) -> i32 {
		cond :: p(a, r);
		if cond {
			return a;
		} else {
			u :: f(a);
			y :: h(u, a);
			return y;
		}
	}

	t :: h(x, 1);

	return t;
}



